const primaryColor = {
  main: '#0AA6C9',
  soft: '#D3F1F8',
}

const secondaryColor = {
  main: '#FBB829',
  soft: '#FDEED0',
}

const infoColor = {
  main: '#59B8E6',
  soft: '#D0EDFB',
}

const successColor = {
  main: '#00C851',
  soft: '#D1EFDD',
}

const dangerColor = {
  main: '#FF4444',
  soft: '#F9D5D5',
}

const inactiveColor = {
  main: '#9E9D9D',
  soft: '#EBEBEB',
}

export {
  primaryColor,
  secondaryColor,
  infoColor,
  successColor,
  dangerColor,
  inactiveColor,
}
